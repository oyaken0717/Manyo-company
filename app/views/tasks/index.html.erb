<h1>一覧</h1>
<table>
  <tr>
    <th>仕事名</th>
    <th>仕事内容</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name%></td>
    <td><%= task.content %></td>
    <td><%= link_to "内容詳細", task_path(task.id) %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to "仕事登録", new_task_path %>
